import { TIMER } from './types';

export const timer = {
  status: TIMER.STATUS.UNWORKING,
  id: 0,
};

export const elevator = {
  floorCount: 5,
  currentFloor: 1,
};


export default {
  timer,
  elevator,
};
